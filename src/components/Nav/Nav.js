import React from "react";

const Nav = () => {
    return (
        <nav>
            <button>Friends</button>
            <button>Chat</button>
            <button>Other</button>
        </nav>
    )
}

export default Nav;